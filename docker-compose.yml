version: '3'
services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    tty: true
    volumes: 
        - .:/usr/src/app
    ports: 
        - 3500:3000
    environment: 
        - NODE_ENV=development
    
